import Scene from './weather.scene'
import FormLocation from './weather.form.location'
import FormHistory from './weather.form.location'
import GridCountries from './weather.grid.countries'
import GridCities from './weather.grid.cities'
import ClientOWM from './weather.client.owm'

const Weather = {
  Scene,
  FormLocation,
  FormHistory,
  GridCities,
  GridCountries,
  ClientOWM,
}
export default Weather
