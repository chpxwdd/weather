import 'package:flutter/material.dart';

class ScreenHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("History Screen title"),
      ),
      body: Center(
        child: Text("History Screen body"),
      ),
    );
  }
}